// colorInput
// Japanese (ja) localization

(function ($) {
    var localization = $.colorInput.localization["ja"] = {
        cancelText: "中止",
        applyText: "選択"
    };
    $.extend($.colorInput.defaults.components.check, localization);
})(jQuery);