// colorInput
// Italian (it) localization

(function ($) {
    var localization = $.colorInput.localization["it"] = {
        cancelText: "annulla",
        applyText: "scegli"
    };
    $.extend($.colorInput.defaults.components.check, localization);
})(jQuery);