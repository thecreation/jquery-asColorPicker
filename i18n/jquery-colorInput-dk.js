// colorInput
// Danish (dk) localization

(function ($) {
    var localization = $.colorInput.localization["dk"] = {
        cancelText: "annuller",
        applyText: "Vælg"
    };
    $.extend($.colorInput.defaults.components.check, localization);
})(jQuery);