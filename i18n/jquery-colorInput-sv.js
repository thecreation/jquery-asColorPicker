// colorInput
// Swedish (sv) localization

(function ($) {
    var localization = $.colorInput.localization["sv"] = {
        cancelText: "Avbryt",
        applyText: "Välj"
    };
    $.extend($.colorInput.defaults.components.check, localization);
})(jQuery);