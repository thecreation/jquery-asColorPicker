// colorInput
// Finnish (fi) localization

(function ($) {
    var localization = $.colorInput.localization["fi"] = {
        cancelText: "Kumoa",
        applyText: "Valitse"
    };
    $.extend($.colorInput.defaults.components.check, localization);
})(jQuery);